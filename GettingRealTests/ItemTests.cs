using Microsoft.VisualStudio.TestTools.UnitTesting;
using GettingReal.Model.Repositories;
using GettingReal.Model;
using System.Security.Cryptography.X509Certificates;

namespace GettingRealTests;

[TestClass]
public class ItemTests
{
    [TestMethod]
    public void AddItem_ShouldAddItemToRepo()
    {
        /*
        // Arrange

        var repo = new BookRepo(); // BookRepo protected -> public
        var book = new Book(1, "", ,,,,,);

        // Act
        BookRepo.AddItem(book);
        var result = repo.GetLoanById(book.ItemId);

        // Assert
        Assert.IsNotNull(result);
        //Assert.AreEqual(book.ItemId, book.Name, result.ItemId, result.Name, result....);
        */
    }
}
